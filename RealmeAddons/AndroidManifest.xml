<?xml version="1.0" encoding="utf-8"?>
<!--
 Copyright (C) 2025 The LineageOS Project

 SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lineageos.settings.realme"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".RealmeAddonsApplication"
        android:label="@string/app_name"
        android:persistent="true"
        android:allowBackup="false"
        android:theme="@style/Theme.SubSettingsBase.Expressive">

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".service.BootCompletedReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Game Optimizer Activity -->
        <activity
            android:name=".ui.game.GameOptimizerActivity"
            android:exported="true"
            android:icon="@drawable/ic_game_optimizer"
            android:label="@string/game_optimizer_title">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.system" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_game_optimizer" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/game_optimizer_summary" />
        </activity>

        <!-- Bypass Charging Service -->
        <service
            android:name=".service.BypassChargingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Battery management and charging optimization" />
        </service>

        <!-- Bypass Charging QS Tile -->
        <service
            android:name=".service.BypassChargingTileService"
            android:exported="true"
            android:icon="@drawable/ic_bypass_charging"
            android:label="@string/bypass_charging_tile_title"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>

</manifest>
